{% schema %}
{
  "name": "Bénéfices Forêt",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "BÉNÉFICES CONCRETS"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Une Lumière Qui Rassure"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Découvrez comment nos veilleuses transforment la nuit de votre enfant en un moment de calme et de sérénité grâce à une conception pensée pour le bien-être."
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Bénéfice",
      "limit": 3,
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "sun", "label": "Lumière Constante" },
            { "value": "fox", "label": "Formes Repères" },
            { "value": "aura", "label": "Présence Apaisante" }
          ],
          "default": "sun"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Lumière Constante"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Une lumière constante et non agressive pour un repos sans interruption."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Bénéfices Forêt",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "icon": "sun",
            "title": "Lumière Constante",
            "text": "Une lumière constante et non agressive pour un repos sans interruption."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "fox",
            "title": "Formes Repères",
            "text": "Des formes reconnaissables qui deviennent des repères rassurants dans la nuit."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "aura",
            "title": "Présence Apaisante",
            "text": "Une présence visuelle apaisante, même dans l'obscurité la plus totale."
          }
        }
      ]
    }
  ]
}
{% endschema %}

<style>
/* Section 3 - Bénéfices Forest */
.forest-benefits-section {
  background-color: #081A32;
  padding: 100px 4rem;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.forest-benefits-container {
  max-width: 1440px;
  margin: 0 auto;
}

.forest-benefits-header {
  margin-bottom: 4rem;
  max-width: 800px;
}

.benefits-badge {
  color: #E89D46;
  font-size: 0.94rem;
  font-weight: 700;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  margin-bottom: 1rem;
  display: block;
}

.forest-benefits-h2 {
  font-size: clamp(2.2rem, 3.85vw, 3.3rem);
  color: #F9F9F9;
  font-weight: 700;
  margin: 0 0 1.5rem 0;
  letter-spacing: -0.02em;
}

.forest-benefits-description {
  font-size: 1.26rem;
  color: rgba(249, 249, 249, 0.7);
  line-height: 1.6;
  max-width: 700px;
}

.benefits-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 32px;
}

.benefit-card {
  background: rgba(255, 255, 255, 0.02);
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 32px;
  padding: 40px;
  text-align: left; /* Alignement à gauche selon la maquette */
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  transition: all 0.4s cubic-bezier(0.25, 1, 0.5, 1);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  display: flex;
  flex-direction: column;
}

.benefit-card:hover {
  transform: translateY(-8px);
  border-color: rgba(232, 157, 70, 0.3);
  background: rgba(255, 255, 255, 0.04);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
}

.benefit-icon {
  margin-bottom: 2rem;
  width: 60px;
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(232, 157, 70, 0.1);
  color: #E89D46;
  filter: drop-shadow(0 0 10px rgba(232, 157, 70, 0.3));
  transition: filter 0.3s ease;
}

.benefit-card:hover .benefit-icon {
  filter: drop-shadow(0 0 20px rgba(232, 157, 70, 0.6));
}

.benefit-icon svg {
  width: 28px;
  height: 28px;
}

.benefit-title {
  color: #F9F9F9;
  font-size: 1.25rem;
  font-weight: 600;
  margin: 0 0 1rem 0;
}

.benefit-text {
  color: rgba(249, 249, 249, 0.7);
  font-size: 1.1rem;
  line-height: 1.6;
  margin: 0;
}

@media screen and (max-width: 1024px) {
  .benefits-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media screen and (max-width: 768px) {
  .forest-benefits-section {
    padding: 80px 2rem;
  }
  .benefits-grid {
    grid-template-columns: 1fr;
  }
  .benefit-card {
    padding: 32px;
  }
}
</style>

<section class="forest-benefits-section">
  <div class="forest-benefits-container">
    
    <div class="forest-benefits-header">
      {% if section.settings.badge_text != blank %}
        <span class="benefits-badge">{{ section.settings.badge_text }}</span>
      {% endif %}
      
      {% if section.settings.title != blank %}
        <h2 class="forest-benefits-h2">{{ section.settings.title }}</h2>
      {% endif %}
      
      {% if section.settings.description != blank %}
        <p class="forest-benefits-description">{{ section.settings.description }}</p>
      {% endif %}
    </div>

    <div class="benefits-grid">
      {% for block in section.blocks %}
        <div class="benefit-card" {{ block.shopify_attributes }}>
          <div class="benefit-icon">
            {% case block.settings.icon %}
              {% when 'sun' %}
                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <circle cx="12" cy="12" r="4.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                  <path d="M12 2.5V4M12 20V21.5M4.5 12H3M21 12H19.5M6.7 6.7L5.5 5.5M18.5 18.5L17.3 17.3M6.7 17.3L5.5 18.5M18.5 5.5L17.3 6.7" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" opacity="0.5"/>
                </svg>
              {% when 'fox' %}
                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M12 19C15.5 19 18 16.5 18 13C18 9.5 15.5 7 12 7C8.5 7 6 9.5 6 13C6 16.5 8.5 19 12 19Z" stroke="currentColor" stroke-width="1.2"/>
                  <path d="M8 8L6 4M16 8L18 4" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/>
                  <circle cx="10" cy="12.5" r="0.75" fill="currentColor"/>
                  <circle cx="14" cy="12.5" r="0.75" fill="currentColor"/>
                </svg>
              {% when 'aura' %}
                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <circle cx="12" cy="12" r="8" stroke="currentColor" stroke-width="0.8" stroke-dasharray="1 3" opacity="0.4"/>
                  <circle cx="12" cy="12" r="4" stroke="currentColor" stroke-width="1.2"/>
                  <path d="M12 9.5V14.5M9.5 12H14.5" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" opacity="0.7"/>
                </svg>
            {% endcase %}
          </div>
          
          <h3 class="benefit-title">{{ block.settings.title }}</h3>
          <p class="benefit-text">{{ block.settings.text }}</p>
        </div>
      {% endfor %}
    </div>

  </div>
</section>
